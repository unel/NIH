


NIH.STORAGE.rqDo(
    ['jquery']
    ($) ->
        $('.b').on('click'
             ->
                NIH.STORAGE.rqDo(
                    ['jquery-ui', 'naked-ass']
                    ($, img_url) ->
                        $("<div><img src='#{img_url}' width='255px'></div>").dialog();
                    );
        )
);